<!-- <h1>New Post</h1>

<%= form_for @post do |f| %>
<%= image_tag @post.image_url.to_s, class: 'bg' %>

<div class="content">
  <h1><%= @post.title %></h1>
  <p>
    <%= @post.content %>
  </p>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_field :content %>

  <p>
  <%= f.label :image %><br>
  <%= f.file_field :image %>
  </p>
  <%= f.submit "Create" %>

</div>

<% end %> -->
