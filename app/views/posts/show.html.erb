<p id="notice"><%= notice %></p>
<%= image_tag @post.image_url.to_s, class: 'bg' %>


<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>


<div class="content">
<h1><%= @post.title %></h1>
<p>
  <%= @post.content %>
</p>
</div>

<%= link_to 'Edit', edit_post_path(@npost) %> |
<%= link_to 'Back', posts_path %>
